{
  "name": "pnyl-debate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "db:local": "wrangler d1 execute church-untold --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute church-untold --remote --file=./schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
